# R067 行内代码支持执行计划

## 范围

仅补齐编辑框和 NoteCard 的行内代码体验，复用现有组件、Code mark 和主题样式。

## 任务

- [x] 确认范围并记录澄清与设计。
- [x] 补齐纯文本粘贴中的单行行内代码转换，避免代码内标签 decoration。
- [x] 验证编辑与展示语义，运行测试和两种生产构建。
- [x] 更新验证记录、提交并推送。

## 状态

实现完成，等待用户验收，不自动归档。

## 实际改动与验证

补齐纯文本行内代码粘贴、转义反引号的卡片展示与编辑恢复，并跳过代码中的标签 decoration。复用既有行内代码 CSS 和主题 token，无新增依赖。真实 Tiptap 测试复现旧序列化问题并验证逐字输入，首页测试覆盖粘贴输出、卡片显示和重新编辑。全量 26 个测试文件、161 项测试通过；Backend 与 Supabase 生产构建通过，无超大 chunk 警告。未进行浏览器视觉验证，等待用户检查截图中的笔记。
