# R070 移除无用帮助按钮

首页侧栏顶部的帮助按钮未绑定操作，移除该入口，减少无效交互。复用现有工具栏容器，数据源操作和主题切换依次排列；桌面侧栏与窄屏抽屉共用此结构。

```text
修改前：ℤ 工作区名称    [帮助] [数据源操作] [主题切换]
修改后：ℤ 工作区名称           [数据源操作] [主题切换]
```

实际改动为删除 HomePage 中的帮助按钮及不再使用的 Button、CircleHelp 导入，删除英文、简体中文和繁体中文的 composer.help 文案。不新增组件、依赖或抽象。
