# r054-solid-send-icon 设计文档

日期：2026-08-29

## 方案

复用 `NoteEditor` 已有的 `SendHorizontal` 图标和提交语义，将创建与编辑笔记共用的发送按钮改为透明的图标点击区域。图标填充当前强调色，移除圆角、背景、阴影与按钮内边距；保留现有可访问性名称、禁用状态和 `--icon-hit-size` 点击范围，不新增组件、状态或依赖。

## 验证

运行完整前端测试和两种生产构建，确认发送行为与产物均保持正常。
